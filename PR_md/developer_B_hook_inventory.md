# Week 8 Memory v1 — Developer B Hook-Point Inventory (Prework)

Working root:
- `/home/jiawei2022/BME1325/week8/merge`

This document is **prework**: it identifies precise integration points for Memory v1.
It does **not** implement `app_core/memory/` and must not create a second schema or storage.

## User Mode Hook Points (app_core/app/api_v1.py)

Source file:
- `/home/jiawei2022/BME1325/week8/merge/app_core/app/api_v1.py`

Key: line numbers below are from `nl -ba` snapshots taken on 2026-04-24.

| event_type (taxonomy) | code location | trigger condition | structured_facts (minimum) | notes |
|---|---|---|---|---|
| `encounter_started` | `user_mode_chat_turn` around L1150-L1180 | First time a non-smalltalk message yields a non-empty `chief_complaint` and session enters the clinical flow | `chief_complaint`, `symptoms`, `arrival_mode` (walk-in), `memory_enabled` (from env), `run_id` (user session), `patient_id` | Do not fire on pure greetings; use the first valid complaint intake point. |
| `calling_nurse_called` | `user_mode_chat_turn` around L1179-L1196 | INTAKE -> `CALL_NURSE_MEASURE` transition, calling nurse handoff message enqueued | `call_status`, `movement_suggestion.target_zone` | This corresponds to “number calling / vitals station”. |
| `vitals_measured` | `user_mode_chat_turn` around L1197-L1210 | After `_nurse_measured_vitals(...)` and `shared_memory['vitals']` is updated; measurement note enqueued | `vitals` (spo2/sbp/pain_score if any) | This is a key checkpoint for safety floor (but memory must not own safety). |
| `triage_completed` | `user_mode_chat_turn` around L1211-L1236 | After `start_encounter(...)` returns; triage line enqueued | `triage` (acuity_ad/ctas/zone/hooks/max_wait), `encounter_id` | Must include isolation keys: `run_id/mode/encounter_id/patient_id`. |
| `doctor_assessment_started` | `_maybe_auto_progress` around L853-L881 and green-channel block around L1237-L1257 | When session enters `DOCTOR_CALLED` and doctor opening is enqueued | `triage`, `queue_position`, `estimated_wait_minutes` | There are two routes: (1) green channel bypass; (2) waiting queue -> called. |
| `doctor_assessment_checkpoint` | in DOCTOR_CALLED branch around `_update_stage2_trace(...)` L1371-L1377 | After protocol retrieval + plan validate + trace update | `primary_protocol_id`, `secondary_protocol_ids`, `filled_slots`, `missing_critical_slots`, `evidence_refs` (if any), `active_target` | This is the best place to reduce repeated questions later (bounded retrieval). |
| `disposition_decided` | in DOCTOR_CALLED readiness branch around L1436-L1499 | When doctor decides disposition and emits `doctor_disposition` | `target_system` (ICU/WARD/OUTPATIENT), `acuity`, `doctor_data` summary fields | Must not be generated by LLM; hard rules own disposition. |
| `handoff_requested` | in disposition branch around `request_handoff(...)` L1445-L1454 | When handoff ticket is created | `handoff_ticket_id`, `target_system`, `reason` | This should also trigger `HandoffMemorySnapshot (requested)`. |
| `handoff_completed` | `_maybe_auto_progress` in `BED_NURSE_FLOW` around L882-L914 | After `complete_handoff(...)` returns; bedside nurse enqueues bed info | `receiver_system`, `receiver_bed`, `transfer_latency_seconds` | Also write `HandoffMemorySnapshot (completed)`. |
| `encounter_closed` | end-of-flow paths: phase becomes `DONE` or session_completed message enqueued around L1503+ | Encounter/session ends | `final_phase`, `final_state` (if any), `handoff` (if any) | This must be the last event for replay slicing. |

## Auto Mode Hook Points (reverie/backend_server/*)

Goal for auto hooks in Week8 MVP (B-side): capture collaborator week7 realism events without changing auto simulation policies.

### Key explicit event: boarding timeout

Source file:
- `/home/jiawei2022/BME1325/week8/merge/reverie/backend_server/persona/persona_types/patient.py`

Location:
- Around L193-L217 (inside `Patient.move(...)`): sets `data_collection['boarding_timeout_event']` and calls `utils.log_runtime_event("boarding timeout event recorded", extra={...})`.

Mapping:
- `boarding_timeout` event_type
- structured_facts: `patient`, `timestamp`, `threshold_minutes`, plus run metadata (run_id/mode/encounter_id)

### Centralized runtime event bridge candidate (inventory only)

Source file:
- `/home/jiawei2022/BME1325/week8/merge/reverie/backend_server/utils.py`

Location:
- `log_runtime_event(...)` around L54-L65

Rationale:
- auto already emits rich `message + extra + sim_code + (optional) step` runtime logs.
- For Week8 MVP we can map a *subset* of these logs into memory events (after A provides the service), while keeping this function “fail-open”.

Suggested log-to-event mapping (MVP):
- message contains `boarding timeout` -> `boarding_timeout`
- message contains `bottleneck`/`capacity`/`waiting` (lab/imaging) -> `resource_bottleneck`
- otherwise: do not write (avoid taxonomy explosion) or write a single `resource_bottleneck` only when `extra` includes queue/capacity fields.

## Integration Guardrails (must remain true)
- Do not change doctor planner ownership.
- Do not change `next_slot` ownership.
- Do not change safety floor ownership.
- Do not change disposition ownership.
- Do not modify `EDSIM_MODE` mode boundaries.
- Do not implement `app_core/memory/` substrate here.

